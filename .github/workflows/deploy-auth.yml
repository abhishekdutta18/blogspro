name: Deploy Auth Worker

on:
  push:
    branches:
      - main
    paths:
      - 'scripts/auth-proxy-worker.js'
      - 'wrangler.toml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: 'npm'

      - name: Deploy Auth Worker to Cloudflare
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          npx wrangler@3 deploy --env auth

      - name: Push Auth Worker secrets
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          FIREBASE_WEB_API_KEY: ${{ secrets.FIREBASE_WEB_API_KEY }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          GITHUB_CLIENT_ID: ${{ secrets.GITHUB_CLIENT_ID }}
          GITHUB_CLIENT_SECRET: ${{ secrets.GITHUB_CLIENT_SECRET }}
        run: |
          [ -n "$FIREBASE_SERVICE_ACCOUNT" ] && echo "$FIREBASE_SERVICE_ACCOUNT" | npx wrangler@3 secret put FIREBASE_SERVICE_ACCOUNT --env auth || echo "FIREBASE_SERVICE_ACCOUNT not set — skipping"
          [ -n "$FIREBASE_WEB_API_KEY" ] && echo "$FIREBASE_WEB_API_KEY" | npx wrangler@3 secret put FIREBASE_WEB_API_KEY --env auth || echo "FIREBASE_WEB_API_KEY not set — skipping"
          [ -n "$SESSION_SECRET" ] && echo "$SESSION_SECRET" | npx wrangler@3 secret put SESSION_SECRET --env auth || echo "SESSION_SECRET not set — skipping"
          [ -n "$GOOGLE_CLIENT_ID" ] && echo "$GOOGLE_CLIENT_ID" | npx wrangler@3 secret put GOOGLE_CLIENT_ID --env auth || echo "GOOGLE_CLIENT_ID not set — skipping"
          [ -n "$GOOGLE_CLIENT_SECRET" ] && echo "$GOOGLE_CLIENT_SECRET" | npx wrangler@3 secret put GOOGLE_CLIENT_SECRET --env auth || echo "GOOGLE_CLIENT_SECRET not set — skipping"
          [ -n "$GITHUB_CLIENT_ID" ] && echo "$GITHUB_CLIENT_ID" | npx wrangler@3 secret put GITHUB_CLIENT_ID --env auth || echo "GITHUB_CLIENT_ID not set — skipping"
          [ -n "$GITHUB_CLIENT_SECRET" ] && echo "$GITHUB_CLIENT_SECRET" | npx wrangler@3 secret put GITHUB_CLIENT_SECRET --env auth || echo "GITHUB_CLIENT_SECRET not set — skipping"

  notify-failure:
    needs: deploy
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: Send Telegram Failure Alert
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          RUN_ID: ${{ github.run_id }}
          REPO: ${{ github.repository }}
        run: |
          [ -n "$TELEGRAM_BOT_TOKEN" ] && curl -s "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=🚨 [BlogsPro] Auth Worker Deploy Failed%0Ahttps://github.com/${REPO}/actions/runs/${RUN_ID}" \
            -d "disable_web_page_preview=true" || echo "Telegram not configured"
