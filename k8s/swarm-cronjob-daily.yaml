# [V1.0] BlogsPro Daily Swarm Tome - Institutional Strategic Synthesis
apiVersion: batch/v1
kind: CronJob
metadata:
  name: daily-swarm-tome
  namespace: blogspro
spec:
  schedule: "0 0 * * *" # Every day at 00:00 UTC
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: blogspro-sa
          containers:
          - name: swarm-engine
            image: us-central1-docker.pkg.dev/blogspro-ai/blogspro-repo/swarm-engine:latest
            args:
            - "node"
            - "scripts/generate-institutional-tome.js"
            - "--freq=daily"
            - "--type=briefing"
            - "--force"
            envFrom:
            - secretRef:
                name: blogspro-secrets
            resources:
              requests:
                cpu: "500m"
                memory: "1Gi"
              limits:
                cpu: "2"
                memory: "4Gi"
          restartPolicy: OnFailure
